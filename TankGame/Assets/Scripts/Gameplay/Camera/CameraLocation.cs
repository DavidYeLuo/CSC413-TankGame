using UnityEngine;

namespace Gameplay.Camera
{
    /**
     * This is just a Tag component to indicate where to place the camera when initializing.
     * Without this tag, the player creation won't be find and instantiate the camera.
     */
    public class CameraLocation : MonoBehaviour
    {
        
    }
}